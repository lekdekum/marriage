services:
  postgres:
    image: postgres:16-alpine
    container_name: marriage-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-marriage}
      POSTGRES_USER: ${POSTGRES_USER:-marriage}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-marriage_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-marriage} -d ${POSTGRES_DB:-marriage}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
